id: s3_child
namespace: exomachina.s3
# Deliberate attempt to overwrite revision 1. The API's response is evidence.
revision: 1
tasks:
  - id: research
    type: io.kestra.plugin.core.debug.Return
    format: child-v2
  - id: source_check
    type: io.kestra.plugin.core.debug.Return
    format: source-check-v2
outputs:
  - id: marker
    type: STRING
    value: "{{ outputs.research.value }}"
