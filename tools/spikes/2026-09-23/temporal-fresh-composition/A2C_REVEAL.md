# Withheld A2C arrangement

Revealed **after** the evaluator verified `freeze.json`: 21 frozen files, inventory SHA-256 `77956a0f7ded1113401017b90e1358b2bd86d951559122e083d3526663d3b83d`. The visible end-to-end probe had already passed. The evaluator sent the arrangement to the implementer only after that freeze boundary.

Author a new four-branch child factory document with two source branch instances and two counter branch instances, assigned to separately running black-box Strands/A2A capability services if the frozen Adapter permits. Counter A reports `clear`; counter B reports `requires_scope`. Delay counter B until the other three assignments finish. The typed join must retain all four exact branch digests, both source claim sets, and both counter objection sets.

Route on the typed Boolean `join.requires_scope` **after** join and **before** synthesis. The true path creates an unresolved first draft; the false path creates a resolved first draft; both converge on independent Quality. A bounded two-repair path uses the pinned run outcome input (`from_run`) for repair synthesis.

One immutable mixed-scope package must produce both outcomes solely by varying run input: r1 reject → r2 Quality acceptance → one exact acceptance and release; and r1/r2/r3 rejection → nested Director wait → authorized current-r3 abort, with zero acceptance and release. A separate all-clear revision, with both counters clear, must take the false route and accept/release r1 directly. An old visible revision must remain waiting through the publications and worker restart, then complete under its old digest. Prefer original Director A2A Task IDs for the mixed paths.

Do not change the frozen executor, validator, Adapter, publisher, supervisor, dependencies, or visible-probe source after reveal. If the candidate cannot express or execute this arrangement, record the exact blind failure. Any later fix must be separate from the frozen result.
