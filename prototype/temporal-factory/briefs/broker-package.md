# Lane: Node model broker package (tw_package, pane wG:p7)

Read `prototype/temporal-factory/INTERFACES.md` first, especially "Model broker lane"; it is binding. Then read `/tmp/exomachina-pi-strands-debate/matched/strands-broker-interleaved/{broker.mjs,interleaved-result.md}` and `/tmp/exomachina-pi-strands-debate/matched/strands-broker/worker-audit.md` (its limit 5 lists the broker defects to fix: stale lock, unconditional socket unlink, `^` range). Work only in `prototype/temporal-factory/`.

Files you own: `broker/package.json`, `broker/package-lock.json`, `broker/exo-model.mjs`, any `broker/lib/*.mjs`, `broker/test/*.test.mjs`, and one new line `prototype/temporal-factory/broker/node_modules/` in the repo-root `.gitignore`. Read-only: everything else. `broker/testing/` belongs to tw_quality (mock Codex backend + OAuth token-endpoint preload); they are building it now in parallel. Write your tests against the protocol in INTERFACES and coordinate by reading their files; if you need a fixture change, write the request in your handoff rather than editing their files.

Required:
1. Package: exact pin `0.87.1`, lockfile, `npm ci` into `broker/node_modules` (try `npm ci --offline --cache /tmp/exomachina-pi-strands-debate/npm-cache` first). Use only public pi-ai exports (`@earendil-works/pi-ai`, `/providers/openai-codex`, `/api/openai-codex-responses`, public `Models`, `CredentialStore` shape, `provider.auth.oauth.refresh`). No forks, no `node_modules` edits.
2. `exo-model.mjs serve|login|status|refresh|logout|leak-scan` exactly as INTERFACES specifies. Singleton with proven-stale recovery for both socket and credential lock (dead-pid check). `login` defaults to device code by answering pi-ai's login `select` prompt with `device_code`; `--browser` uses the PKCE loopback flow. Record in your handoff that pi-ai's browser authorize URL hard-codes `originator=pi` and whether the device flow sends any originator.
3. The credential never leaves the Node process: no token in stdout/stderr, `broker-events.jsonl`, socket replies, or error text. Redact errors. `authContext` returns nothing for env and files (no `OPENAI_API_KEY`, no `~/.codex`, no `~/.pi`). Reject a non-`oauth` credential type with `reauth_required`. Map pi-ai `ModelsError` codes and HTTP 401/403 to `reauth_required`; 429/usage-limit to `rate_limit`/`quota`.
4. Honest identity headers through the public `fetch` option; egress allowlist; loopback-only `EXO_CODEX_BASE_URL`.
5. Node tests (`node --test broker/test/`), loopback only, `EXO_MODEL_HOME=/tmp/exo-proto-broker-<suffix>/model`, ports 46100–46119: singleton attach, stale socket and stale lock recovery, owner-only modes, `status`/`health` without token material, stream relay with interleaved fragments against the mock, cancel on client close, forced `refresh` through the preload (new expiry persisted atomically, old refresh token not reused), `reauth_required` on missing/invalid credential with no fallback even when `OPENAI_API_KEY` is set in the env, originator/User-Agent headers observed by the mock, egress refusal of a non-allowlisted host, and `leak-scan` finding a planted token in a SQLite file while printing no token.

Do not sign in to a real account and do not contact chatgpt.com or auth.openai.com; the live sign-in is a later, user-performed step. Do not delete anything. Do not commit.

Handoff: `handoff/broker-package.md` — files, exact commands with pass/fail output, the originator findings, known gaps.
