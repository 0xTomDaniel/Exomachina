# Lane: Python broker client, Strands Model, provider selection (tw_version, pane wG:p6)

Read `prototype/temporal-factory/INTERFACES.md` first, especially "Model broker lane" and "Authoring lane"; it is binding. Then read `/tmp/exomachina-pi-strands-debate/matched/strands-broker-interleaved/{pi_broker_model.py,interleaved-result.md}` (the proven `contentIndex` ordered-fragment fix and index-restoring reasoning replay) and `matched/strands-broker/worker-audit.md`. Work only in `prototype/temporal-factory/`.

Files you own: `src/model_broker.py` (new), `tests/test_model_broker.py` (new), and in `src/authoring.py` only `model_from_environment` plus whatever is needed so `AuthoringOutcome.model` carries `{kind, id, provider, billing, live}` correctly for broker models. In `src/admin.py` add only a `model-status` subcommand (broker `health` + `status`, no tokens) and make `author` report the selected provider and refusal reason. Keep `tests/test_authoring.py` passing; update only its `model_from_environment` expectations. Read-only: everything else. `broker/` (Node) is tw_package's and is being written in parallel; code against the socket protocol in INTERFACES.

Required:
1. `ModelBroker` lifecycle exactly as INTERFACES: lazy, idempotent attach-or-spawn of a detached `node broker/exo-model.mjs serve`, readiness via `run/broker-ready.json` + `health`, `reason` recorded, no work at import time. Nothing in harness startup may start the broker.
2. `PiBrokerModel`: port the interleaved spike adapter faithfully (per-index routing, queued later blocks, reject missing/reused/mismatched/unfinished indices, reasoning at `done` with original index, lossless `to_pi` replay). Add `SubscriptionAuthRequired` for `reauth_required`, `ModelThrottledException` for `rate_limit`/`quota`, `BrokerLost` for a connection lost before a terminal event, cancel on `CancelledError`/`GeneratorExit`. `structured_output` may raise `NotImplementedError`.
3. `model_from_environment` per INTERFACES: default `codex-subscription` uses the broker only and never inspects API keys; explicit `anthropic|bedrock|openai-api` keep today's checks; `synthetic-loopback` requires a loopback `EXO_CODEX_BASE_URL`. Signed-in check goes through broker `health` (starting the broker lazily is acceptable here because authoring is factory work). `live: true` only for `codex-subscription` without a base-URL override.
4. Tests (`$PY -B -m unittest tests.test_model_broker -v`) with an in-test fake broker on a Unix socket under `/tmp/exo-proto-pybroker-<suffix>/` (Python fake only; the Node mock belongs to tw_quality): interleaved fragments reach Strands in block order with correct association; a real `strands.Agent` tool loop over the fake completes; reasoning replay restores the original index order; `BrokerLost`, throttling and `SubscriptionAuthRequired` mapping; cancel sent on close; provider selection never returns an API-key model when `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` are set but provider is default and not signed in; the model record labels.

Python: `/Users/tomdaniel/Documents/Ember_Cognition_Inc/Software/Exomachina/tools/spikes/2026-09-22/arbitration/temporal/.venv/bin/python`. Full suite must still pass: `$PY -B -m unittest discover -s tests`. Do not contact any real provider. Do not delete anything. Do not commit.

Handoff: `handoff/broker-python.md` — files, exact commands with pass/fail output, known gaps.
