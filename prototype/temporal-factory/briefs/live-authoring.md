# Lane: live-authoring integration scenario (tw_director, pane wG:p5)

Read `prototype/temporal-factory/INTERFACES.md` first (all of it; "Model broker lane" is new and binding) and `README.md`. Then read `scenarios/integrated.py`, `scenarios/common.py`, `src/admin.py`, `src/authoring.py`, and `/tmp/exomachina-pi-strands-debate/matched/strands-broker-interleaved/{scenario.py,interleaved-result.md}`. Work only in `prototype/temporal-factory/`.

Files you own: `scenarios/live_authoring.py` (new), additive helpers in `scenarios/common.py` (do not change existing helper behaviour; `integrated.py` must keep working), and `evidence/live-authoring-*.json` plus any `evidence/live-authoring-*/` directory. Read-only: everything else. In parallel, tw_package is building `broker/exo-model.mjs`, tw_version `src/model_broker.py` + provider selection, and tw_quality `broker/testing/mock-codex.mjs --script authoring`. Write your scenario against the INTERFACES contracts now; run it as the pieces land (poll for their `handoff/broker-*.md` files).

Required:
1. `live_authoring.py --home H --provider {synthetic-loopback,codex-subscription} [--max-rounds N]` performs the path in INTERFACES "Live authoring scenario" on the ordinary factory-mode harness: testbed up, provision, publish v1, `admin.py author` with the broker model and **without** `--allow-scripted`, auto-approval, publish v2, v2 run via A2A `message/send` on the instance's normal identity, poll to terminal, Temporal history export for the v2 workflows, `node broker/exo-model.mjs leak-scan` over `H`, the evidence file, exported histories, and `git diff` output. Model home: `EXO_MODEL_HOME` is `H/model` for synthetic; for `codex-subscription` it is the default install-wide home (do not override it), because the user signs in once there.
1a. **Amendment (audit, binding):** `leak-scan` excludes exactly the canonical credential file and nothing else, so zero hits is achievable. Each run must include the positive control (plant a copy of the credential file and the bare access-token string elsewhere under a control directory, show both are detected, record it in evidence). In `codex-subscription` mode, exit non-zero before any broker start, sign-in check or request if `EXO_CODEX_BASE_URL` is set in the inherited environment; never set it yourself. Add a test proving that rejection happens before any HTTP request (a recording loopback server observes zero requests). Label the release step `http-release (fixture)`; it is not A2A.
2. Synthetic mode starts `broker/testing/mock-codex.mjs --script authoring` on a 4613x port with `EXO_CODEX_BASE_URL` pointing at it, and a synthetic credential written into `H/model/secrets/openai-codex.json` (0600) by test code only. Label everything from it `synthetic`.
3. Evidence records, per claim, `level: real|synthetic`: model record, authoring rounds with draft digests and validation errors, tool-call sequence, broker `health` (no tokens), mock-recorded headers (originator, user-agent), approval, v2 publication and build, A2A task states, Temporal status/versioning, broker start reason (must be lazy: absent before authoring), leak-scan result, and wall times. A failure is recorded as such, with the step that failed; do not mask it.
4. Assert: harness startup did not start broker or runner; the broker started lazily on authoring; the authored v2 differs from v1 and validates; v2 ran on its pinned build through A2A; zero leak-scan hits; no API-key provider was consulted.

Run synthetic end to end now: `$PY -B scenarios/live_authoring.py --home /tmp/exo-proto-live-syn-<n> --provider synthetic-loopback`. Do **not** run `codex-subscription` or contact chatgpt.com/auth.openai.com until the orchestrator tells you sign-in is done. Python: `/Users/tomdaniel/Documents/Ember_Cognition_Inc/Software/Exomachina/tools/spikes/2026-09-22/arbitration/temporal/.venv/bin/python`. Do not delete anything; stop anything you start (runner via `src/runner.py stop --home H`). Do not commit.

Handoff: `handoff/live-authoring.md` — files, commands, pass/fail output, evidence paths, gaps.
