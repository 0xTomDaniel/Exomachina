# Lane: Strands authoring agent (pane wG:p7)

Read `prototype/temporal-factory/INTERFACES.md` first; it is binding. Work only in `prototype/temporal-factory/`.

Goal: `src/authoring.py` implements the authoring Interface. A Strands agent produces and revises a new graph definition, using validation feedback, into an approved package that the factory instance can publish as v2. Lane-1 `author.py` only materialized hand-written templates, which does NOT count as agent authoring. The loop must be driven by an agent calling tools.

Files you own: `src/authoring.py`, `tests/test_authoring.py`, `definitions/authoring-brief-v2.md` (the brief text for v2). Read-only: `src/definition.py` (another worker may edit it concurrently. Use only `validate`, `digest`, `ALLOWED`, `RESULT_TYPES`, `ROUTE_VALUES`, `INPUT_TYPES`, and `INPUT_SOURCES`), `src/harness_server.py` (see `ToolCallingModelFixture` for how a fixture Strands `Model` streams tool calls), and `definitions/v1-template.json`.

Required:
1. Implement everything under "Authoring lane" in INTERFACES.md. `StrandsGraphAuthor` uses a real `strands.Agent` with `@tool` functions. The session enforces the round cap. `submit_draft` validates the materialized package against `approved_bindings` and returns structured errors on failure. Every round is recorded.
2. `model_from_environment()` checks, without printing secrets and without prompting: `ANTHROPIC_API_KEY` plus an importable `anthropic` package (strands AnthropicModel), `AWS_*`/`~/.aws` plus boto3 for Bedrock, and `OPENAI_API_KEY` plus `openai`. The orchestrator already observed that none are available here. Return `(None, reason)`, and make the live path report `untested: <reason>`. Do not install packages and do not ask for credentials.
3. `ScriptedAuthoringModel(strands.models.Model)` streams tool calls, not text: `describe_vocabulary`, then `validate_draft(draft with a real defect, e.g. a route missing a typed case or repair without exhausted edge)`, then parses the returned error and emits a corrected draft, then `submit_draft`. The corrected draft must be derived from the error it received, not a precomputed constant for round 2. It is labelled `live: false` everywhere.
4. The v2 graph (brief in `definitions/authoring-brief-v2.md`) is a *different* approved composition from v1. A good target: 3 parallel branches (`source_alpha`, `source_beta`, `counter_alpha` with `scope_status: "clear"`), a join, a route on `join.route_status`, synthesize, Quality, a verdict route, bounded repair (max 1), Director wait → abort on exhaustion, release → complete, all inside the nested child with the standard root. It must validate with the v1 approved bindings. Include run inputs `question` (as in v1). If there's no `from_run` synthesis, no `outcome_mode` is needed.
5. Tests: the scripted session produces ≥2 rounds, and round 1 is invalid with a specific error. The final package validates, and `package_digest` differs from the v1 package. The round cap is enforced. `submit_draft` rejects an unknown binding and an arbitrary node type. `model_from_environment` returns None here with a reason. Use fixture approved bindings (the same shape as `definition.validate` expects: `{name:{role,url:"http://127.0.0.1:<port>",identity,approved:true}}` for source_alpha, source_beta, counter_alpha, counter_beta, quality, release).

Run: `PY=/Users/tomdaniel/Documents/Ember_Cognition_Inc/Software/Exomachina/tools/spikes/2026-09-22/arbitration/temporal/.venv/bin/python; cd prototype/temporal-factory && $PY -B -m unittest tests.test_authoring -v` (add `src` to sys.path inside the test).

Handoff: `handoff/authoring.md`, including the round-by-round transcript of one scripted session (tool calls, errors, digests), saved as `evidence/authoring-scripted-session.json`.
