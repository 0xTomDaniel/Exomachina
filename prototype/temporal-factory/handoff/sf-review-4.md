# Independent review 4 — single-factory live attempt 3

24 September 2026. Read-only review at integration HEAD `cd13b914f49820116cbe0203ec8d9cc49b3d964b`, using preserved `/tmp/exo-sf-live-3`. The only write is this handoff. No broker operation, provider call, service, runner, or scan was started. SQLite was opened only with `file:...?mode=ro`; I did not query `identity`, open credential files, or decode raw histories under `evidence-raw`. R1-d below is **structural only**, as A1 assigns semantic reading to the orchestrator.

The saved live file has 24/24 `pass`, `status: structural-pass`. Its separate post-write attestation has `g4_final.pass: true` for the SHA-256 of the current final JSON. Under `handoff/sf-director.md`'s “Review 3 fixes” rule, the attempt qualifies structurally; G-6 and R1-d semantic reading remain separate.

## Findings

| ID | Severity | Check | Claim | Evidence path + field | Suggested fix |
| --- | --- | --- | --- | --- | --- |
| F1 | note | G-4, G-7 | Review-3 final-byte gap is resolved for the live attempt. The separate manifest names the current final JSON and all 20 unique referenced report/history/store files, with matching SHA-256s; it also names all 10 live exports, including console. It records 2,245 scanned files, zero hits and a fresh detected control. Its own output is necessarily outside its input manifest. | `codex-subscription-3-attestation.json`: `evidence_sha256`, `manifest`, `scan_summary`, `positive_control`, `g4_final.terms`; current `codex-subscription-3.json` bytes. | None for this attempt. Keep the attestation beside the exact evidence bytes. |
| F2 | note | G-4 | Review-3 Director-token lookup is gone. The scenario's read-only `SELECT *` helper is called for `tasks`, `model_calls`, `stimulus_log` and `outcomes`, never `identity`; neither scenario nor attester opens a credential file. All 10 current live exports, 32 decoded base64 JSON payloads and 101 redaction objects pass an independent structural scan, including nested JSON strings. This supports absence of exported sensitive values; it is not proof about every possible secret shape. | `scenarios/single_factory.py:797-811,921-1005,1009-1012`; `scenarios/sf_attest.py:65-113`; live attestation `redaction`; exported live JSON/histories/reports/console. | None for the stated structural claim. Keep the synthetic positive control clearly separate from live credentials. |
| F3 | note | SF-0 | Review-3 missing artifacts are present and used by SF-0: preflight `lstat` recorded `FileNotFoundError`, the served card body hashes to its saved SHA and has one skill, and the saved broker status fields include `signed_in:true`, `expired:false` and an account hash. The preflight and status remain contemporaneous collector observations; prior nonexistence cannot be replayed from the present home. | `codex-subscription-3.json`: `setup.preflight_home_lstat`, `served_factory_card.body/sha256`, `broker_status_fields`; `scenarios/single_factory.py:341-360`. | None for this run; retain the observation fields. |
| F4 | note | R2-b/c/d, R3-b/d/e | Review-3 behavior labels now say the defect was induced, repair was assigned, and abort was the only permitted decision. Live Quality still identified the planted claim on all four induced candidates. Attempt 3's route-3 claim/finding is `C5` (attempt 2 used `C6`); route 2 still accepts `r2` (`k=2`). Director tool sequences are unchanged from attempt 2: `start_research, inspect_run` on each start, then `inspect_run, decide_wait` on route-3 follow-up. Raw model response text is not retained, so model attribution rests on Task-bound live calls, stored artifacts, parser behavior and `decided_by:model`. | Live evidence `checks.*.behavior`, `agents.quality.tasks`, `routes.*.director_calls`; attempt-2 evidence same fields; raw `model_calls` and Director tool-call store. | Continue using the qualified labels; retain response digests if stronger forensic attribution is desired. |
| F5 | note | R1-d, G-6 and other coverage | A1's semantic report reading and G-6 suites remain outside the 24 scenario checks. Packet provenance is independently 10/10 line-span matches at `2d609e3`, allowing terminal newline trimming; this is an observation, not semantic proof. The separate A2A contract limitations noted in review 3 remain outside this review. | `briefs/single-factory.md` A1/G-6; live `semantic_reading.status`; attestation `packet_provenance`; packet `items[*].source/text`. | Record the orchestrator's semantic reading and G-6 gate separately. |
| N1 | minor | G-7 ancillary export | The current `scripted-12-console.txt` is absent from `scripted-12-attestation.json`'s 9-file export scan and manifest. It is not referenced by the scripted evidence, and the later live attestation includes it as a commit candidate; therefore this does not flip G-7. Do not describe the scripted attestation alone as binding that current console file. An independent structural scan of the current 10 scripted exports found no violation. | `scripted-12-attestation.json`: `manifest`, `redaction.files_checked: 9`; current `scripted-12-console.txt`; live attestation `manifest[path=.../scripted-12-console.txt]`. | If the scripted console needs its own final-byte claim, attest it explicitly as an additional file; preserve the existing G-7 binding as historical evidence. |

Severity counts: **0 blocker, 0 major, 1 minor, 5 notes**. No observed secret leak or unsupported live route verdict.

## Independent cross-checks

- **Final bytes and prerequisite.** Live evidence SHA-256 is `770b99b76c4b865919a2012f662acbb3fdf6138fbbfb9927a70578a3161bd6cb`, exactly the attestation's `evidence_sha256` and manifest entry. The 332-entry live manifest has no missing referenced path; all 20 unique referenced files and all 10 live exports have matching current hashes. I independently hashed 330 manifest entries; two credential-themed handoff documents were left unopened under this review's credential-file restriction. The scan implementation recursively visits each regular file passed to it and excludes only the broker's canonical credential. The recorded scan has zero hits and a detected new positive control. The live checker and attester SHA fields match current code bytes. The scripted prerequisite is `structural-pass`, 24/24, with final evidence SHA `753feef156f837ba2e9e055946c94b8f3f05e310e810af4ce7308b2a57f94eb8`, a passing attestation, matching attestation-file SHA `c7d7a87a7174058c94dff994fcdae95ceb5a2a98d3074a0c7472bbea5a0def83`, and the same checker SHA. Its different manifest digest is expected because the two fresh homes have distinct bindings; the interpreter build matches.
- **Raw Task stores.** Four read-only agent stores exactly match the evidence's copied `tasks`, `model_calls` and `stimulus_log` rows. They contain 3 findings, 3 risks, 6 synthesis and 6 Quality Tasks: 18 distinct IDs, all completed, in mutually exclusive stores. Each has exactly one completed `codex-subscription` / `gpt-6-sol` / `live=1` call and a unique session. Longest observed Task duration is 17.03 s, within 3 calls/240 s. Five service identities, ports, state directories and store paths are distinct in the saved testbed/journal bindings; `identity` tables were not queried.
- **Pinned version and topology.** The raw Director `runs` store has exactly three caller runs, each closed, on build `b-3e196d3f362c`, manifest `6cbb2bd8ef61016cf3ab8dd03bd98154341c202ee4a0cd38dd30d329f8d2bd47` and package `e9a67de893c71ac2ad717138e8558484ff802bfaae595f3749d4b101e6167919`. Six redacted parent/child histories name the correct workflow IDs and the same tuple; their start events show `PINNED_OVERRIDE_BEHAVIOR_PINNED` on that build. Child scheduled activities are 6/8/9 for routes 1/2/3, with release only on 1/2. I did not decode raw histories or independently re-run their pre-redaction control audit. All caller messages and 18 raw Task briefs are free of the registered stimulus/control keys.
- **Revisions and releases.** Raw synthesis content SHA-256s, artifact IDs, Quality `candidate` objects, distinct author/reviewer identities, original Task DataParts, receipts and raw release rows reconcile exactly. Route 1 accepts/releases `r1` SHA `7df919cdda0bde9c0278989e60f4dbf01ec34dce9ea3e7b8adea5553228de864`; route 2 rejects `r1` SHA `244de091fa704033df6b693ce494249ecb187776c0770c71f27f269b120562ee` then accepts/releases `r2` SHA `74d6d9193e16b94ca8b190cbc18744018158cc592cc5a30341f752cce0cd9d9e`; route 3 rejects `r1/r2/r3` and releases none. The raw release store has **1/1/0** rows and accepted-effect counts of one on each released row. No rejected SHA is released.
- **Stimulus and repair.** The synthesizer's four raw stimulus-log rows are exactly route 2 `r1` and route 3 `r1/r2/r3`, each bound by run ID, Task ID, revision and final artifact SHA. Each planted text occurs in its candidate claim and markdown. The route-2 blocking finding names planted `C6`; the three route-3 blocking findings name planted `C5`. All six Quality Tasks have live calls on those exact Tasks and `decided_by:model`; the accepted ones have no blocking findings. Each repair brief carries the exact prior SHA/content and preceding Quality findings. Route-2 `r2` has no stimulus row and omits the planted text. Route 3 reaches `repair_count=max_repairs=2`, `repair:exhausted`, then `awaiting-director`/`input-required` before abort.
- **Director, journal and reports.** The raw Director store has 3 runs, 4 live turns, 8 accepted tool calls and zero incidents. Each turn uses 3 model calls and 2 tools, with saved elapsed times 24.642, 11.447, 8.628 and 9.274 s, all within 4/4/90. The route-3 follow-up text exactly matches the registered text on the original Task/context; its tools are `inspect_run` then `decide_wait(abort,r3,current SHA)`. The Task completes aborted with no artifact. The raw outcome journal has exactly 20 rows, all `confirmed`, identical to the saved rows, distributed 5/7/8 by route. Route-1 and route-2 markdown files equal their accepted synthesis markdown and original Task TextParts byte for byte. Route 1 has five claims citing packet IDs; its four required sections have 75/37/63/33 prose words, satisfying the structural A1 minimum without judging semantics.
- **Authoring, broker and cleanup.** One valid authoring round used `validate_draft` before `submit_draft` with the same draft digest; approval, sole catalog publication, active package/manifest and five approved binding names reconcile. The served card body recomputes to its saved SHA and has one skill; the installed packet digest matches its canonical packet. All 18 agent sessions appear exactly once among 34 recorded broker streams; the other 16 reconcile to 4 authoring and 12 Director model calls. The broker PID is recorded as `28404` before, in all seven during samples and after (no in-window start, 35 attaches). Cleanup records 10 started processes/groups exited, five stopped services, stopped runner, no listeners on the 39 checked ports and no errors. These temporal observations are preserved records, not a new live probe.

## Per-check disposition

| Check | Agree/disagree | Decisive observation |
| --- | --- | --- |
| SF-0 | **Agree** | Saved preflight `FileNotFoundError`, one factory, served one-skill card with recomputed SHA, signed-in/unexpired hashed-account status and clean required environment fields. |
| SF-1 | **Agree** | One valid authoring round and ordered validate/submit; four live authoring streams; approved package equals sole active publication, pinned packet and five bindings. |
| SF-2 | **Agree** | Five distinct identities/ports/state directories, four exclusive Task stores plus release, verified card-pin activity preceding interactions, and clean stated import-audit scope. |
| SF-3 | **Agree** | Three distinct caller/child runs, six correct parent/child exports, one pinned build/manifest/package tuple, text-only caller messages. |
| R1-a | **Agree** | Original Task observed `working→completed`; one live start turn and one accepted `start_research` (plus an accepted inspection). |
| R1-b | **Agree** | Two completed, exclusive research Tasks, exact artifact SHA/action/run/author bindings, valid packet IDs and distinct live sessions. |
| R1-c | **Agree** | Live synthesis `r1` and Quality Task accept the exact `7df919cd…` candidate; `decided_by:model`, reviewer distinct from author. |
| R1-d | **Agree, structural only** | One exact-SHA release and one original Task artifact; saved markdown equals raw synthesis/Task text; five cited claims and substantive required sections. Semantic reading remains pending. |
| R1-e | **Agree** | Sole route-1 Quality verdict is a live model acceptance; no rejection or stimulus row. |
| R2-a | **Agree** | One raw stimulus row only on route-2 `r1`, bound to final Task SHA; raw briefs/caller/Director fields contain no control key. Redacted history-input audit is a saved observation. |
| R2-b | **Agree** | Live Quality Task rejects exact route-2 `r1` SHA with a blocking `C6` finding on the induced claim. |
| R2-c | **Agree** | Assigned repair brief binds prior content/SHA/findings; live synthesis creates unplanted, different `r2`. |
| R2-d | **Agree** | Live Quality accepts `r2` (`k=2`); one release, receipt and original Task artifact bind its exact SHA; rejected `r1` is unreleased. |
| R3-a | **Agree** | Three raw stimulus rows bind route-3 `r1/r2/r3` final Tasks and SHAs; none on route 1. |
| R3-b | **Agree** | Three live Quality rejections bind exact candidates and blocking planted `C5`; repair count reaches 2/2 and exhaustion is recorded. |
| R3-c | **Agree** | Original Task observed `input-required`, with child wait phase `awaiting-director`. |
| R3-d | **Agree** | Exact registered follow-up on original Task/context; follow-up turn inspects then aborts current `r3` SHA; completed-aborted Task has no report or release. |
| R3-e | **Agree** | Four turns remain within 4 calls/4 tools/90 s each; constrained abort label is accurate. |
| G-1 | **Agree** | All 20 raw outcomes equal saved confirmed journal rows; no Director incident. |
| G-2 | **Agree** | Stable recorded PID; 18 unique agent sessions each occur once in broker streams, plus 4 authoring and 12 Director streams. |
| G-3 | **Agree** | Every raw agent Task uses one live call and at most 17.03 s, within the 3-call/240-s budget. |
| G-4 | **Agree** | Final-byte live attestation passes; all current live exports and decoded JSON payloads have valid structural redaction; recorded scan has zero hits and a fresh detected control. |
| G-5 | **Agree** | Ten recorded process/group exits, valid service/runner stops, no listeners on 39 ports and unchanged broker PID. |
| G-7 | **Agree, with N1 scope** | `scripted-12.json` and its passing attestation are hash-bound, share the live checker SHA/build, and have 24 structural passes; its later console is covered by the live attestation, not its own. |

**Verdict:** All 24 saved live verdicts are supported at their stated structural scope, and `structural-pass` plus the matching `g4_final.pass` satisfies the final verdict rule. **I would flip no check.** This does not decide R1-d semantic usefulness or G-6.
