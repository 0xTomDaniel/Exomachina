# Independent review 3 — single-factory live attempt 2

24 September 2026. Read-only review of integration `92628b7` and the preserved `/tmp/exo-sf-live-2` trial. The only write was this handoff. No broker operation, provider call, service, runner, or test was started. SQLite connections used `file:...?mode=ro`; no credential or Director token value was selected or printed. I did not read the credential-bearing raw Temporal histories. I checked their preserved SHA references and inspected the redacted exports.

Scope: the 24 saved verdicts in `evidence/single-factory/codex-subscription-2.json`, its six redacted histories, reports and console, `evidence/single-factory/scripted-10.json`, the brief and Amendment A1, and the raw Task, release, journal and Director stores. R1-d below is **structural only**; its A1 semantic reading is outside this review.

## Findings

| ID | Severity | Check | Claim | Evidence path + field | Suggested fix |
| --- | --- | --- | --- | --- | --- |
| F1 | **major** | G-4; consequently G-7 | The same-run `leak-scan` covers the actual final commit-candidate bytes. It does not cover its own final evidence JSON: the collector scans and hashes an intermediate JSON, then writes cleanup, checks and final status to that file. Both the live and scripted final JSON SHA-256 differ from their own candidate-manifest entry. This is a coverage failure, **not a detected secret**. | `evidence/single-factory/codex-subscription-2.json` and `scripted-10.json`: `leak_scan.candidate_manifest[path=<self>].sha256` versus each final file SHA-256; `scenarios/single_factory.py:1279,1340-1362,1428-1432`. The live scan recorded 2,648 files, 290 candidate entries and zero hits; the scripted scan has the same self-hash problem. | Finalize the evidence, then run an independent post-write candidate scan with its result in a separate attestation whose scanned-file SHA manifest matches the delivered files. Re-evaluate G-4 and G-7 using that attestation. |
| F2 | **major** | G-4 | The literal pre-registered statement “no token was read or printed by scenario or worker code” is contradicted for the **Director token**: the scenario reads the `identity` table, extracts `token`, and searches exported/candidate files with it. I did not read that value. This does not show that a broker credential was read or that any token was printed. | `briefs/single-factory.md` G-4; `scenarios/single_factory.py:1344-1348`; live evidence `leak_scan.director_token_absent` and `decoded_payload_token_absent`. | If the prohibition means broker credentials only, amend the claim to say so and describe the Director-token comparison explicitly. Otherwise replace the comparison with a check that does not read the token. |
| F3 | **minor** | SF-0 | Fresh-home existence before launch, broker sign-in/account hash, and the served factory card's one skill are saved as collector observations, without separate preflight/card/status artifacts for independent replay. The present home and stores support the one-factory topology, but cannot establish prior nonexistence. | Live evidence `setup.fresh_home`, `setup.broker_status`, `setup.card_skill_count`; `/tmp/exo-sf-live-2/instances/report-factory/instance.json`. | Preserve redacted preflight output, served card digest/body and signed-in status response, or label these as collector observations. |
| F4 | **note** | R2-b/R3-b, R2-c/R2-d, R3-d/R3-e | “Spontaneous” accurately describes the live Quality verdict content and live repair content **after an induced claim and explicit repair assignment**. It does not mean the defects or repair trigger were spontaneous. The route-3 model issued the abort tool call, but the Director prompt and graph both permit only abort at `repair_exhausted`; “caller-prompted, model-decided” is accurate only in that constrained procedural sense, not as an unconstrained choice of outcome. Raw model response text was not retained; the live-call rows, stored verdicts, and role/parser code provide the attribution. | Live agent `model_calls`, Quality `tasks.artifact`, synthesizer `tasks.brief` and `stimulus_log`; `services/agent_roles.py:174-205,242-267`; `src/director_agent.py:163-170`; live route 3 `director_calls`. | Keep the induced and constrained qualifiers beside these labels. For stronger forensic attribution, preserve redacted raw model responses or response digests tied to each Task. |
| F5 | **note** | Coverage outside the 24 | A1's R1-d semantic reading and G-6 suite gate are expressly separate from the 24 emitted checks. The brief's packet-excerpt provenance, A2A idempotent replay/conflicting-payload rejection, failure-to-incident route, and unsupported A2A operations also have no end-to-end check in this evidence. The packet's ten excerpts do match the cited `2d609e3` line spans apart from terminal newline trimming; that is my read-only cross-check, not a saved scenario check. | `briefs/single-factory.md:55-69,82-96,238-250,277-291`; live `semantic_reading.status: pending-orchestrator-reading`; live `checks` keys; packet at `/tmp/exo-sf-live-2/instances/report-factory/evidence_packet.json`. | Track these as separate evidence/coverage items. Do not turn the structural R1-d pass or synthetic route controls into semantic or live proof. |

## Independent cross-checks

- The four agent stores contain 18 completed, mutually exclusive Task IDs: research findings 3, research risks 3, synthesis 6, Quality 6. Every Task has one `codex-subscription` / `gpt-6-sol` / `live=1` call, a distinct session, and a completed outcome. The 18 sessions each appear once among 34 broker stream events; the other 16 reconcile to 4 authoring and 12 Director calls. All observed Task durations are at most 16.45 s. Five stored service identities and ports are distinct; the release store holds two rows.
- The raw `runner/outcomes.sqlite3` has exactly the 20 exported action IDs, with JSON rows identical to the saved journal; all are `confirmed`. The Director store has three caller runs, four live turns, twelve accepted tool calls, and zero incidents. The release store has one accepted `r1` effect for route 1, one accepted `r2` effect for route 2, and none for route 3. Their exact SHAs match the synthesis artifacts, Quality acceptances, receipts, and original Task DataParts.
- The synthesizer's four stimulus-log rows bind to the final artifact SHAs: route 2 `r1` only, route 3 `r1/r2/r3`, none on route 1. Each planted text occurs once as claim `C6` and in markdown. Live Quality rejects those four candidates with a blocking `C6` finding that specifically identifies the false live/fixture claim. Route 2's `r2` repair brief includes exact `r1` prior content/SHA and findings, and the unplanted `r2` is accepted. Route 3 repair briefs likewise bind each prior rejection; final child status has `repair_count=max_repairs=2` and `repair:exhausted`.
- Six exported histories have matching recorded redacted SHA-256s. Their start events have the correct parent/child workflow IDs, child-to-parent links and `PINNED_OVERRIDE_BEHAVIOR_PINNED` on build `b-3e196d3f362c`; all saved workflow summaries have the same manifest and package digests. Activity sequences are six for route 1, eight for route 2, nine for route 3, with release only in routes 1 and 2. All three caller inputs are text-only; the route-3 follow-up is the exact registered text on the original Task/context.
- The saved route-1 and route-2 markdown files equal their original Task TextParts and accepted synthesis `markdown` byte for byte. Route 1 has five claims citing packet IDs, the usefulness helper says `ok`, and `semantic_reading` is pending. I did not assess semantic usefulness.
- The five service bindings in the active publication equal the testbed bindings, and the installed packet's canonical SHA-256 equals the pinned packet digest. Authoring has one valid round, `validate_draft` before `submit_draft`, four broker streams, a live subscription model record, matching approved/published package digest, and one catalog publication. The release receiver is explicitly labelled `http-release (fixture)`; no live release target is claimed.
- A read-only scan of the final exported live JSON, six redacted histories, two markdown reports and console found no unredacted sensitive-key value or credential-format literal. Recursive decoding of 33 base64 JSON payloads in the live JSON/histories found no unredacted sensitive key. This is narrower than an exact Director-token comparison, which this review did not perform. The recorded scanner reports zero real hits and a detected positive control.
- Cleanup records ten started service/harness/runner processes or groups, all exited; five services report stopped, runner stopped, 39 live-block ports had no listener, and the broker PID was `28404` before, during and after. The scripted prerequisite file SHA matches its live binding; scripted provider, checker SHA, interpreter build and all 24 saved scripted check flags match. The scripted-to-live git difference adds only prerequisite evidence and suite logs in this prototype tree.

## Per-check disposition

| Check | Disposition | Decisive observation |
| --- | --- | --- |
| SF-0 | **Agree, with F3 limit** | One factory-mode instance in the preserved home; saved fresh-home, one-skill, signed-in and environment observations. |
| SF-1 | **Agree** | One valid authoring round and ordered validate/submit; four live broker streams; approved digest equals active sole publication with packet and five approved bindings. |
| SF-2 | **Agree** | Five distinct stored identities/ports/state directories; four exclusive agent Task stores plus release; 350 sends/polls have preceding same-action pin events; running-service import audit is clean. |
| SF-3 | **Agree** | Six correct parent/child histories, one pinned build/manifest/package tuple, and text-only caller messages. |
| R1-a | **Agree** | Original Task `working→completed`; one live turn, one accepted `start_research` on that Task. |
| R1-b | **Agree** | Research Task IDs `c4eba9aa…` and `d8e2746d…` completed in their own stores, with matching artifact hashes, journal IDs, valid packet items and distinct live sessions. |
| R1-c | **Agree** | Live synthesis `r1` SHA `bde8feda…`; live Quality Task `a2def83f…` accepts that exact revision/SHA with `decided_by:model` and a distinct reviewer identity. |
| R1-d | **Agree, structural only** | One release and one original Task artifact bind `bde8feda…`; saved markdown equals Task TextPart and accepted synthesis markdown; five cited claims and structural helper pass. Semantic reading remains pending. |
| R1-e | **Agree** | The sole route-1 Quality verdict accepts `r1`; no rejection or stimulus row. |
| R2-a | **Agree** | Exactly one route-2 `r1` stimulus row binds Task `d5cffeb8…` and SHA `a47ad625…`; the saved structured control audit reports no factory/caller control field. |
| R2-b | **Agree** | Quality Task `5e557a75…` has one live model call, rejects candidate `a47ad625…`, `decided_by:model`, with blocking `C6` finding on the induced claim. F4 qualifies “spontaneous.” |
| R2-c | **Agree** | `r2` repair brief carries exact `r1` prior SHA/content and findings; Task `22deab47…` has a live call, new SHA `0f131c48…`, and no planted text or stimulus row. |
| R2-d | **Agree** | Live Quality Task `9e932cdc…` accepts `r2` SHA `0f131c48…`; one release and one original Task artifact name that SHA; rejected `r1` was not released. |
| R3-a | **Agree** | Three and only three route-3 stimulus rows bind `r1/r2/r3` Tasks and final artifact hashes; route-1 has none. |
| R3-b | **Agree** | Three live model Quality Tasks reject their exact `r1/r2/r3` candidates with blocking `C6`; child repair count is 2/2 and completed path includes `repair:exhausted`. |
| R3-c | **Agree** | Original Task was observed `input-required`; preserved wait status is `awaiting-director` at `r3`. |
| R3-d | **Agree, with F4 qualifier** | Exact caller follow-up on original Task/context; same live turn accepted `inspect_run` then `decide_wait(abort,r3,7ede653b…)`; completed-aborted Task has no artifact or release. |
| R3-e | **Agree, with F4 qualifier** | Director turn counts are 1/1/2; each has three model calls, two tool calls and under 25 s, within 4/4/90. |
| G-1 | **Agree** | All 20 raw journal outcomes equal saved rows and are confirmed; no Director incident. |
| G-2 | **Agree** | One broker PID in all seven samples, 18 distinct agent sessions each in a stream; other 16 streams reconcile to authoring/Director 4/12. |
| G-3 | **Agree** | All 18 raw agent Tasks have one model call and duration at most 16.45 s, within 3 calls/240 s. |
| G-4 | **Disagree** | F1: final evidence bytes were not in the recorded candidate scan. F2: the literal no-token-read clause conflicts with the scenario's Director-token lookup. No exported secret was detected by this review. |
| G-5 | **Agree** | Ten recorded processes/groups exited, valid stop results, no listeners on 39 ports, unchanged broker PID. |
| G-7 | **Disagree, consequential to F1** | The scripted file is real and correctly bound, with 24 recorded passes, but its own final JSON is also absent from its recorded same-run candidate hash/scan. Thus “every structural check” is not independently established under G-4's stated coverage condition. |

**Verdict:** 22 of the 24 recorded live checks are supported at their stated scope; I would flip **G-4 and G-7** pending a post-final scan/attestation and clarification of the token-read rule. The three core route behaviours, exact revision bindings and 1/1/0 release counts are supported. This does not decide R1-d semantic usefulness or G-6.
